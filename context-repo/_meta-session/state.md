# Session State

> **Política:** retrato do que está **em andamento agora** — não acumula histórico (isso é
> papel do `journal.md`). **Sobrescrito por completo** ao fim de cada cascade que altera este
> estado; nunca faça append aqui. Se nada está em andamento, as seções ficam vazias (não
> apague os cabeçalhos).

## AYD/SPEC ativos
- AYD-002 (`draft`) — meta-AYD de robustez do framework; será removido ao fim do ciclo.
- SPEC-006 (`review`) — memória de sessão (journal + state), implementada nesta sessão.
- SPEC-001, SPEC-007, SPEC-008, SPEC-009 (`draft`) — ainda não implementadas.

## Reviews pendentes
- SPEC-002, SPEC-003, SPEC-004, SPEC-005, SPEC-006 aguardando revisão humana para `approved`.

## Próximos passos
- Implementar SPEC-001 (validador), SPEC-007 (init skill), SPEC-008 (evals) e SPEC-009
  (agentes de execução) — são as SPECs restantes de AYD-002.
